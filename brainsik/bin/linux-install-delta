#!/usr/bin/env bash
#
# Installs the latest version of delta on Linux.
# - https://github.com/dandavison/delta
#
set -eu -o pipefail
repo="dandavison/delta"
libc="gnu"
exec "$HOME/bin/linux-install-ghrepo-release-binary" "$repo" "$libc"
