[git_branch]
symbol = " "

[git_status]
modified = "!${count}"
staged= "+${count}"
ahead = "⇡${count}"
diverged = "⇕⇡${ahead_count}⇣${behind_count}"
behind = "⇣${count}"

[python]
python_binary = ["python3", "python"]

[sudo]
disabled = false

[terraform]
format = "🪐[$version]($style) "

[time]
disabled = false
format = "[$time]($style) "
